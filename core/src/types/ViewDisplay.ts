export interface ViewDisplay {
	cameraOverride: {
		positionX: number;
		positionY: number;
		positionZ: number;
		rotationX: number;
		rotationY: number;
		fov: number;
	};
	windowWidth: number;
	windowHeight: number;
}
